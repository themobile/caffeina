ion-view(title='inventory',ng-controller="inventory",ng-init='init()')
    ion-content(padding='true',scroll='true')

        row.screenHeader
            .col.screenTitle My photo tools
        .row
            .col {{inventory.length}} items > swipe item left for actions

        ion-list.inventoryList
            ion-item.inventory.item-icon-left(ng-repeat='item in inventory', item='item')
                i.icon.ion-ios7-gear
                h4 {{item.name}}
                p.small <strong>serial:</strong>&nbsp{{item.serial}}
                p.small <strong>date:</strong>&nbsp{{item.date | date: 'yyyy | MMM d' }}
                ion-option-button.button-assertive(ng-click='deleteItem(inventory[$index])') Delete
                ion-option-button.button-energized(ng-click='editItem(inventory[$index],$index)') Edit